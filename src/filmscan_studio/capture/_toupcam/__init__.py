"""Vendored Touptek SDK (see README.md). Not part of the public API.

Import through ``filmscan_studio.capture.touptek``, which wraps this and
keeps the ctypes surface (and its native library) out of every other module.
"""
